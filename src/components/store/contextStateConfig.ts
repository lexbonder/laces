// import { useReducer } from 'react';
// import XPContext from './context';
// import { initialState, xpReducer } from './reducers/xpReducer';
// import { addXP, reduceXP } from './actions/actions';

// const ContextState = () => {
//     const [state, dispatch] = useReducer(xpReducer, initialState);

//     const handleAddXp = (amount: number) => {
//         dispatch(addXP(amount));
//     };

//     const handleReduceXp = (amount: number) => {
//         dispatch(reduceXP(amount));
//     };

//     return <XPContext.Provider></XPContext.Provider>;
// };
